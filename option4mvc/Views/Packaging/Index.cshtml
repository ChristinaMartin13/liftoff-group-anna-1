@model List<Packaging>

@{
    ViewData["Title"] = "Packaging List";
}



<div class="text-center">
    <h1 class="display-4">Packaging List</h1>

    @if (Model.Count == 0)
    {
        <p>No items in inventory yet!</p>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Price</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Description</td>
                        <td>@item.PackagingPrice</td>
                        <td>@item.Quantity</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

<p><a asp-controller="Packaging" asp-action="Create">Create Packaging Item</a></p>

<p><a asp-controller="Packaging" asp-action="Delete">Delete Packaging Item</a></p>

<p><a asp-controller="Packaging" asp-action="Edit">Edit Packaging Item</a></p>

